

test:
  step1:
    container:
      image: alpine:latest
      cmds:
      - echo 'Hello world!'
      - echo 'output from somefile.txt:'
      - cat somefile.txt
  step2:
    container:
      image: busybox:latest
      cmds:
      - date

